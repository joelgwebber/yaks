---
id: yak-5892
title: 'View model: status tabs become an ordered View list'
type: feature
priority: 2
created: '2026-08-16T23:15:40Z'
updated: '2026-08-16T23:24:37Z'
parent: yak-4473
labels:
- ui
---

Tranche 1 of the View-list arc (yak-4473); pure refactor, no visible behavior change — the de-risker everything else builds on.

Introduce a View: a named (FilterSpec + sort_by + sort_dir + limit + layout=tree|flat) that yields a task list. Replace self.tab:int with self.view:int indexing an ordered self.views list; tab_counts -> view_counts. Seed self.views with the three built-in status Views (Hairy/Shaving/Shorn) — un-deletable, always first. Rendering (draw_tabs, TABS) reads from the views list instead of the hardcoded triple.

Done when: the TUI looks and behaves exactly as today (three status tabs), but the tab machinery is driven by the View list. Touches tui.py, yaktui/render.py; keep the diff mechanical.

---
▸ 2026-08-16T23:24:37Z
Done. Introduced yaktui/view.py (View dataclass: name, status, builtin, is_status; builtin_status_views() seeds the three status Views with byte-identical labels). tui.py: self.tab -> self.view indexing new self.views (seeded from builtin_status_views); updated _rebuild_task_list, _handle_mouse tab hit-testing, _switch_tab, _navigate_to. render.py: removed TABS; tab_counts -> view_counts (now a list aligned to app.views, index-keyed, memo key gains a views signature); added view_tab_text() as the single source of tab-strip text used by draw_tabs, mouse hit-testing, and inline-search cursor placement. mutate.py: app.tab -> app.view. Bonus: centralizing tab text via view_tab_text fixed two latent width inconsistencies from 3fd4.4 (mouse hit-test and inline-search cursor both used the raw, uncapped count and the cursor path also omitted the * marker) so clicks/cursor now align even at counts >999. Behavior identical to before (three status tabs). tests/test_counts.py updated to view_counts/index-keyed + a view_tab_text cap/marker test. Full suite 144 pass; import + label smoke test green; no new ruff findings.
