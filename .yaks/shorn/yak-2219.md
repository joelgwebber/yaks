---
id: yak-2219
title: 'Research: Rust conversion'
type: task
priority: 3
created: '2026-08-16T15:14:11Z'
updated: '2026-08-20T02:18:40Z'
labels:
- rust
---

Parent yak for research into converting yaks to Rust.

---
▸ 2026-08-16T23:25:58Z
RESEARCH ARC COMPLETE — all children shorn. Summary of findings:

- ef0f TUI: ratatui 0.30 + crossterm (immediate-mode; deletes ncurses C dep; makes force-repaint yak-be65 moot).
- 6819 EDITOR (biggest pain): edtui 0.11 — embedded modal (vim) + modeless widget rendered INSIDE the frame; deletes the prompt_toolkit shell-out AND the curses-suspend/terminal-takeover in editor.py, and replaces vim_edit.py. tui-textarea as fallback. Helix embedding impractical.
- 1622 STORAGE: keep the yak-3fd4 fs-native, stat-validated, atomically-written DERIVED index (files stay authoritative). No embedded DB now — fjall forbids multi-process, redb is single-process; only LMDB(heed)/SQLite fit our concurrent CLI+TUI, and only for the derived cache if ever measured. Rust makes the current index sub-ms (native ser + rayon rebuild).
- 199b VIRTUAL TERM: one ratatui painter -> TestBackend Buffer drives BOTH live TUI and demo frames (folds in yak-3291); tiny asciicast writer we own; avt (asciinema own Rust VT, which castkit already mirrors by hand) as parse/verify oracle + future GIF/SVG (yak-fa3b).
- 90b8 ECOSYSTEM: clap, serde_yaml_ng/saphyr + hand-rolled frontmatter, notify (fs-events), arboard (clipboard), tempfile/fs4 (atomic write+lock), assert_cmd/insta (tests), cargo-dist/brew/binstall (single-binary distro — the one real downstream change: skill Running-yaks stanza + Claude/Codex marketplace install lines must switch from uvx/PyPI to a binary installer; skill CONTENT unaffected; keep command name yaks).
- 0446 SYNTHESIS: benefits validated — single static binary; MEASURED ~42-48ms/CLI-call today is ~90% Python startup tax (~2-5ms real work) => ~10-20x faster with a Rust binary; better libs, not a lateral port. Incremental migration (Phase 0 spike -> CLI parity -> TUI -> demo -> cutover) is low-risk because fs-native state + per-user index let Rust and Python share one herd during transition.

VERDICT: GO to a Phase 0 spike (yak show/list/next in Rust over the same .yaks/ + existing index), gate full go/no-go on it. Do not block the yak-4473 UI arc on this. Conversion yaks to be spawned on go-ahead, not preemptively.

---
▸ 2026-08-20T02:18:40Z
ROUND 2 (owner refinements) — arc re-shorn. Deltas from round 1:
- Storage (yak-1622): reframed from "embedded DB" to COMPONENT libs. Decision: derived index as an rkyv zero-copy archive (mmap + stat-validate + atomic rewrite + rayon rebuild); optional hand-rolled O_APPEND + tombstone segment log for future history (NOT okaywal, which is unstable). Git-like, cross-process-safe, own-the-format. No DB.
- Editor (yak-6819): edtui keymaps as user-selectable profiles (vim/emacs/nano/helix) via config — editing model becomes a user choice.
- Virtual term (yak-199b): the ratatui Buffer / avt path is also a TESTING + agent-introspection surface (headless snapshot + read-back), not just demo generation.
- Distribution (yak-90b8): mirror the proven Go + goreleaser + npm flow via cargo-dist + a Biome-style npm installer (platform packages as optionalDependencies), plus brew/shell/binstall.
- Migration (yak-0446): BOOTSTRAP A NEW REPO (yaks-rs) and port, rather than convert in-place; the two impls interoperate at the data layer, avoiding mixed-language diffs.

NEXT STEP: yak-94e7 (Phase 0 spike, top-level, hairy, gated on go-ahead).
