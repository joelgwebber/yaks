---
id: yak-3373.2
title: 'yaks rollup report + source: ancestor inheritance + --keys (PR-key) mode'
type: feature
priority: 3
created: '2026-06-19T16:46:57Z'
updated: '2026-06-19T19:44:17Z'
depends_on:
- yak-3373.1
parent: yak-3373
---

Build the one new piece of code. (1) Effective source: a yak with no own source inherits its nearest ancestor's. (2) 'yaks rollup [filter]' groups yaks by effective source and lists which yaks sit under each external target (read-only, no MCP/network). (3) '--keys' mode prints just the distinct external keys for a shipping set (e.g. --label pr-369) so they can be pasted into a PR body for native forge linking.

---
▸ 2026-06-19T19:44:17Z
Built rollup. New module yaklib/rollup.py: tracker_and_key() classifies jira/linear/github/other source URLs to a human key; effective_source() resolves a yak's source by walking up the parent chain (own beats inherited; query-time only, descendants never stamped); build_rollup() groups filtered yaks by effective source. cmd_rollup in commands.py + 'rollup' parser entry (FilterSpec flags + --keys + --json) + COMMANDS entry + commands/rollup.md slash command. --keys prints distinct external keys for PR bodies. 13 new tests (tracker_and_key, effective_source, CLI grouping/inheritance/keys/filter/empty). 107 tests pass.
