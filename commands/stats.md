---
description: "Show task statistics"
argument-hint: "[--json]"
allowed-tools:
  - Bash
---

Run the following command to show task statistics:

```
python3 ${CLAUDE_PLUGIN_ROOT}/scripts/yak.py stats $ARGUMENTS
```

Shows counts by status, type, and priority.
